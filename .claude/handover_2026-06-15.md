# DARKSWORD — Session Handover

**Prepared:** 2026-06-15 (late) · **By:** SCRIBE · **For:** next session, fresh head
**Gate:** OPS executes only on AO approval. Per-action, never blanket.

---

## 1. ep1153 fell through the scheduler crack — needs MANUAL ingest · OWNER: OPS (AO gate)

**What:** Ep 1153 published **2026-06-15 14:00:41 -0400**, ~24s *after* the 14:00 auto-run polled RSS and exited (`[14:00:01]` start → `[14:00:17]` finish). The run saw 1152 as latest and exited clean — correct behavior on stale data, not a fault.

**Why it won't self-heal:** Choice 5 compares *latest-episode-date* to *today*. Tomorrow (06-16) the run sees 1153 dated 06-15 ≠ 06-16 → "no new episode" → skips. **Neither today's run (race) nor tomorrow's (date mismatch) catches it.**

**Action:** Manual ingest. Show notes are thin (sponsor links only) → use **Choice 2 (Manual)** or a **transcript path (7/8)**. **NOT Choice 5** — RSS→show-notes would produce empty records. Episode is 97 min, so transcript is the richer source.

**Safe to retry:** `record_exists` dedup guard protects against double-push, so a manual ingest can't dupe existing 1153 records.

---

## 2. ep1152-08 ISC2 record — fixes pending before push · OWNER: OPS (AO gate)

SCRIBE vet complete. Judgment sound; plumbing will bite. Fix before push:

**P0 — silently break on push:**
- `Master Frameworks(CMMC 2.0 / NIST 800-171)::` → rename key to **`cmmc_mapping::`** (used Notion display name as parser key; controls would drop).
- `cpe_credits:: 0.5` → **`0.05`** (10× inflation; CPE audit-integrity issue).
- `GRC_Learning_Plan_All_Phases:: Week 25 - …` → replace hyphen with **en-dash** `Week 25 – Developing security policies` (relation matches by exact title; hyphen ≠ en-dash → links to nothing).

**P1 — vocab trims (schema-cap accretion):**
- `intel_category:: governance, risk-management` → use in-vocab `advisory` / `regulatory`.
- `target_sector:: Cybersecurity Workforce, Enterprise Security Operations, All Sectors` → `all-sectors` (two invented options; target_sector is on the Phase-2 cap watch-list).
- `content_category:: Strategic` → not in current canonical schema; confirm parser still expects it or drop.
- `tags::` → trim novel options (`bite-sized-learning`, `workflow-integrated-learning`, etc.); `tags` is the ~208KB field behind the cap incident.

**P2 — content unverified:** ISC2 stats (≈1,000 leaders / 73% / 98% / 53%) not checked against source. Set **`confidence:: Low`** until verified against the ISC2 report or ep1152 transcript. Also resolve title "Teams" vs body "practitioners" denominator drift.

---

## 3. Librarian's 429 / RESOURCE_EXHAUSTED claim — IGNORE (confabulation)

No log shows a 429. Grep across all `darksword_*.log` = zero hits. Gemini (Choice 8) never ran today; auto path is Choice 5 (RSS), no Gemini call to exhaust. The pasted error trace was narrated, not read. Same class as ep1151 welding: real components (valid Gemini error format, real episode cadence) assembled into a false narrative. **Generation-layer self-assertion is not evidence — logs are.** No action.

> Note: the generic remedy (billing/pay-as-you-go for a true `limit:0`) is correct *in the abstract* — just bolted to a problem that didn't occur.

---

## 4. Scheduler race — real fix needed · OWNER: ARCHITECT (design)

The noon→2pm move **relocated** the publish race, didn't eliminate it (1153 hit 14:00:41, right on the run). Picking a "safer" hour is a timing patch that shifts the collision window. **Proper fix = decouple from the clock:** poll-with-retry/backoff, or feed-triggered ingest. ARCHITECT design call before OPS implements.

*Class of problem: a timing fix that moves a collision instead of removing it.*

---

### Suggested order next session
1. (Quick win) Manual-ingest ep1153 — Choice 2 / transcript path.
2. Apply ep1152-08 P0+P1 fixes, push with `confidence:: Low`, then verify P2 stats.
3. ARCHITECT: design the race fix (poll-retry vs feed-trigger).
4. Drop dead: Librarian 429.
